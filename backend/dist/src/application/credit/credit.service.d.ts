import { PaginationService } from '../../shared/pagination/pagination.service';
import { PaginatedResult, PaginationQueryInput } from '../../shared/pagination/pagination.types';
import type { CreateCreditPlanInput, ICreditRepository, UpdateCreditPlanInput, UpdateCreditSystemConfigInput } from '../../domain/credit/interfaces/credit.repository';
import { CreditPlan } from '../../domain/credit/entities/credit-plan.entity';
import { CreditSystemConfig } from '../../domain/credit/entities/credit-system-config.entity';
import { CreditPurchase } from '../../domain/credit/entities/credit-purchase.entity';
import type { IUserRepository } from '../../domain/user/interfaces/user.repository';
import { PaymentGatewayFactory } from '../../infrastructure/payment/payment-gateway.factory';
export declare const CREDIT_CHECKOUT_REQUESTED_EVENT = "credit.checkout.requested";
export declare const CREDIT_CHECKOUT_FAILED_EVENT = "credit.checkout.failed";
export declare class CreditService {
    private readonly creditRepository;
    private readonly userRepository;
    private readonly paymentGatewayFactory;
    private readonly paginationService;
    constructor(creditRepository: ICreditRepository, userRepository: IUserRepository, paymentGatewayFactory: PaymentGatewayFactory, paginationService: PaginationService);
    getUserBalance(userId: string): Promise<number>;
    getUserTransactionsPage(userId: string, paginationQuery: PaginationQueryInput): Promise<PaginatedResult<unknown>>;
    getUserCreditPurchasesPage(userId: string, paginationQuery: PaginationQueryInput): Promise<PaginatedResult<CreditPurchase>>;
    getActivePlansPage(paginationQuery: PaginationQueryInput): Promise<PaginatedResult<CreditPlan>>;
    createCheckout(userId: string, creditPlanId: string): Promise<CreditPurchase>;
    markCheckoutLookup(purchaseId: string): Promise<CreditPurchase | null>;
    markCheckoutCompleted(purchaseId: string, externalReference?: string): Promise<CreditPurchase>;
    markCheckoutFailed(purchaseId: string, reason: string, externalReference?: string): Promise<CreditPurchase>;
    consumeCredits(userId: string, amount: number, description: string, correlationId?: string): Promise<void>;
    applyRegistrationBonuses(userId: string): Promise<void>;
    listAdminPlansPage(paginationQuery: PaginationQueryInput): Promise<PaginatedResult<CreditPlan>>;
    createAdminPlan(input: CreateCreditPlanInput): Promise<CreditPlan>;
    updateAdminPlan(id: string, input: UpdateCreditPlanInput): Promise<CreditPlan>;
    deleteAdminPlan(id: string): Promise<void>;
    getAdminConfig(): Promise<CreditSystemConfig>;
    updateAdminConfig(input: UpdateCreditSystemConfigInput): Promise<CreditSystemConfig>;
    private tryRewardReferrerOnFirstPurchase;
    private getUserOrFail;
    private validateCreditPlanPayload;
    private validateConfigInput;
    private validatePositiveCredits;
    private validateNonNegativeCredits;
    private validatePriceBrl;
}
